services:
  flutter:
    build:
      context: ./
    tty: true
    volumes:
      - workspace-data:/home/developer/workspace
    # LinuxのFlutterがx64アーキテクチャにしか対応していないため設定
    platform: linux/x86_64

volumes:
  workspace-data:
